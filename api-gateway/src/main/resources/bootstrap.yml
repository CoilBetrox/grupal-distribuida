server:
  port: 8080
spring:
  application:
    name: service-api-gateway
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: false
        enabled: true
